<%= form_for(@vat) do |f| %>
  <% if @vat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vat.errors.count, "error") %> prohibited this vat from being saved:</h2>

      <ul>
      <% @vat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
