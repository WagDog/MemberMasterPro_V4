<%= form_for(@card_revenue) do |f| %>
  <% if @card_revenue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_revenue.errors.count, "error") %> prohibited this card_revenue from being saved:</h2>

      <ul>
      <% @card_revenue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
