<%= form_for(@rp_sort_group) do |f| %>
  <% if @rp_sort_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rp_sort_group.errors.count, "error") %> prohibited this rp_sort_group from being saved:</h2>

      <ul>
      <% @rp_sort_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sequence %><br>
    <%= f.number_field :sequence %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
